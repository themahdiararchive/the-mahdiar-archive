import a from "@/content/artworks.json"; import d from "@/content/dictionaries.json"; import type {Artwork,Locale} from "./types"; export const artworks=a as Artwork[]; export const locales:Locale[]=["en","fa"]; export const isLocale=(v:string):v is Locale=>locales.includes(v as Locale); export const dict=(l:Locale)=>d[l]; export const art=(id:string)=>artworks.find(x=>x.id===id); export const titleCase=(s:string)=>s.split("-").map(x=>x[0].toUpperCase()+x.slice(1)).join(" ");
