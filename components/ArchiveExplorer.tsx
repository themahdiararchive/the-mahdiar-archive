'use client'; import {useMemo,useState} from "react"; import ArtworkCard from "./ArtworkCard"; import type {Artwork,Locale} from "@/lib/types"; import {dict,titleCase} from "@/lib/content";
export default function ArchiveExplorer({artworks,locale}:{artworks:Artwork[];locale:Locale}){const d=dict(locale);const[q,setQ]=useState("");const[c,setC]=useState("");const[k,setK]=useState("");const cs=[...new Set(artworks.map(a=>a.collection))].sort();const ks=[...new Set(artworks.map(a=>a.category))].sort();const f=useMemo(()=>artworks.filter(a=>{const h=[a.id,a.title.en,a.title.fa,a.year,a.collection,a.category,...a.keywords].join(" ").toLowerCase();return(!q||h.includes(q.toLowerCase()))&&(!c||a.collection===c)&&(!k||a.category===k)}),[artworks,q,c,k]);return <><div className="grid gap-3 md:grid-cols-[1.5fr_1fr_1fr]"><input value={q} onChange={e=>setQ(e.target.value)} placeholder={d.search} className="paper border rule px-4 py-3"/><select value={c} onChange={e=>setC(e.target.value)} className="paper border rule px-4 py-3"><option value="">{d.allCollections}</option>{cs.map(x=><option key={x} value={x}>{titleCase(x)}</option>)}</select><select value={k} onChange={e=>setK(e.target.value)} className="paper border rule px-4 py-3"><option value="">{d.allCategories}</option>{ks.map(x=><option key={x} value={x}>{titleCase(x)}</option>)}</select></div><div className="mt-5 text-sm muted">{f.length} {d.records}</div>{f.length?<div className="mt-8 grid gap-6 md:grid-cols-2 xl:grid-cols-3">{f.map(a=><ArtworkCard key={a.id} artwork={a} locale={locale}/>)}</div>:<div className="mt-8 border border-dashed rule p-12 text-center muted">{d.noResults}</div>}</>}
