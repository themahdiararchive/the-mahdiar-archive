import type {Artwork,Locale} from "@/lib/types"; import {dict,titleCase} from "@/lib/content";
export default function NftSalePanel({artwork,locale}:{artwork:Artwork;locale:Locale}){const d=dict(locale);const n=artwork.nft;const label=n.status==="listed"?d.listed:n.status==="minted"?d.minted:n.status==="sold"?d.sold:d.notMinted;return <section className="mt-10 border border-[var(--gold)] bg-[#eee5d8] p-6"><div className="flex flex-wrap items-center justify-between gap-3"><div><div className="wine text-xs font-bold uppercase tracking-[.2em]">Official NFT record</div><h2 className="serif mt-2 text-3xl">{d.nft}</h2></div><span className="border border-black px-3 py-1 text-xs font-bold uppercase tracking-[.12em]">{label}</span></div><dl className="mt-6 grid gap-3 text-sm sm:grid-cols-2"><div><dt className="muted">{d.chain}</dt><dd className="font-bold">{n.chain}</dd></div><div><dt className="muted">{d.edition}</dt><dd className="font-bold">{n.edition}</dd></div><div><dt className="muted">Standard</dt><dd className="font-bold">{n.standard}</dd></div><div><dt className="muted">{d.royalty}</dt><dd className="font-bold">{n.royaltyPercent}%</dd></div>{n.contractAddress&&<div className="sm:col-span-2"><dt className="muted">{d.contract}</dt><dd className="break-all font-mono text-xs">{n.contractAddress}</dd></div>}{n.tokenId&&<div><dt className="muted">{d.tokenId}</dt><dd className="font-mono text-xs">{n.tokenId}</dd></div>}</dl>{n.status==="listed"&&n.marketplaceUrl?<a href={n.marketplaceUrl} target="_blank" rel="noopener noreferrer" className="mt-6 inline-flex bg-black px-6 py-3 text-white">{d.viewListing}{n.price!==null?` · ${n.price} ${n.currency}`:""}</a>:<p className="mt-6 muted">{d.comingSoon}</p>}<p className="mt-5 border-t rule pt-4 text-xs leading-5 muted">{d.marketplaceNotice}</p></section>}
