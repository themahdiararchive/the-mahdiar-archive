import type {MetadataRoute} from "next"; import {artworks,locales} from "@/lib/content"; export default function sitemap():MetadataRoute.Sitemap{const b="https://mahdiarfarsi.com";return [...locales.flatMap(l=>["","archive","collections","timeline","about"].map(p=>({url:`${b}/${l}/${p}`,lastModified:new Date()}))),...locales.flatMap(l=>artworks.map(a=>({url:`${b}/${l}/archive/${a.id}/`,lastModified:new Date()})))]}
