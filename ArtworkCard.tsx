import Image from "next/image"; import Link from "next/link"; import type {Artwork,Locale} from "@/lib/types"; import {titleCase} from "@/lib/content";
export default function ArtworkCard({artwork,locale}:{artwork:Artwork;locale:Locale}){return <article className="paper border rule transition duration-300 hover:-translate-y-1 hover:shadow-museum"><Link href={`/${locale}/archive/${artwork.id}/`}><div className="relative aspect-[4/3] overflow-hidden"><Image src={artwork.image} alt={artwork.title[locale]} fill className="art-image transition duration-500 hover:scale-[1.03]"/></div><div className="p-5"><div className="wine text-[11px] font-bold uppercase tracking-[.16em]">{artwork.id} · {titleCase(artwork.collection)}</div><h3 className="serif mt-2 text-2xl">{artwork.title[locale]}</h3><p className="mt-2 text-sm muted">{artwork.year} · {artwork.medium[locale]}</p></div></Link></article>}
