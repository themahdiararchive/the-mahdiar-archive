import Link from "next/link"; import {dict} from "@/lib/content"; import type {Locale} from "@/lib/types";
export default function Header({locale}:{locale:Locale}){const d=dict(locale),other=locale==="en"?"fa":"en";return <header className="sticky top-0 z-50 border-b rule bg-[rgba(244,240,232,.94)] backdrop-blur-xl"><div className="shell flex min-h-20 items-center justify-between gap-5"><Link href={`/${locale}/`} className="flex items-center gap-3 text-xs font-bold uppercase tracking-[.16em]"><span className="serif grid h-10 w-10 place-items-center border border-black text-lg">M</span><span>The Mahdiar Archive</span></Link><nav className="hidden items-center gap-7 text-sm md:flex"><Link href={`/${locale}/archive/`}>{d.archive}</Link><Link href={`/${locale}/collections/`}>{d.collections}</Link><Link href={`/${locale}/timeline/`}>{d.timeline}</Link><Link href={`/${locale}/about/`}>{d.about}</Link><Link className="border border-black px-3 py-2" href={`/${other}/`}>{d.language}</Link></nav><Link className="border border-black px-3 py-2 text-xs md:hidden" href={`/${other}/`}>{d.language}</Link></div></header>}
