import Image from "next/image"; import Link from "next/link"; import {artworks,dict,isLocale,locales} from "@/lib/content"; import ArtworkCard from "@/components/ArtworkCard";
export function generateStaticParams(){return locales.map(locale=>({locale}))}
export default async function Home({params}:{params:Promise<{locale:string}>}){const{locale:raw}=await params;if(!isLocale(raw))return null;const locale=raw,d=dict(locale),featured=artworks.filter(a=>a.featured).slice(0,3);return <><section className="shell grid min-h-[78vh] items-center gap-14 py-16 lg:grid-cols-[1.05fr_.95fr]"><div><div className="wine text-xs font-bold uppercase tracking-[.22em]">Official digital museum</div><h1 className="serif mt-5 text-5xl leading-[.98] md:text-7xl xl:text-8xl">{d.hero}</h1><p className="mt-7 max-w-2xl text-lg leading-8 muted">{d.intro}</p><div className="mt-9 flex gap-3"><Link href={`/${locale}/archive/`} className="bg-black px-6 py-3 text-white">{d.enter}</Link><Link href={`/${locale}/about/`} className="border border-black px-6 py-3">{d.about}</Link></div></div><div className="relative p-5"><div className="absolute inset-0 translate-x-3 -translate-y-3 border border-[var(--gold)]"/><div className="relative aspect-[4/5] shadow-museum"><Image src="/artworks/MH-0005.jpeg" alt="Harbour City Study" fill priority className="art-image"/></div></div></section><section className="bg-[#e9e1d5] py-24"><div className="shell"><div className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><div className="wine text-xs font-bold uppercase tracking-[.2em]">{d.featured}</div><h2 className="serif mt-3 text-4xl md:text-6xl">{d.archive}</h2></div><p className="max-w-2xl muted">{d.mission}</p></div><div className="mt-10 grid gap-6 md:grid-cols-2 xl:grid-cols-3">{featured.map(a=><ArtworkCard key={a.id} artwork={a} locale={locale}/>)}</div></div></section></>}
