import ArchiveExplorer from "@/components/ArchiveExplorer"; import {artworks,dict,isLocale,locales} from "@/lib/content";
export function generateStaticParams(){return locales.map(locale=>({locale}))} export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale:raw}=await params;if(!isLocale(raw))return null;const d=dict(raw);return <section className="shell py-20"><div className="wine text-xs font-bold uppercase tracking-[.22em]">Catalogue raisonné</div><h1 className="serif mt-4 text-6xl md:text-8xl">{d.archive}</h1><p className="mt-5 max-w-3xl text-lg muted">{d.mission}</p><div className="mt-10"><ArchiveExplorer artworks={artworks} locale={raw}/></div></section>}
